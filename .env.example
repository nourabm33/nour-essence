# PostgreSQL connection (optional — the app runs with static catalog data when omitted)
DATABASE_URL=postgresql://user:password@localhost:5432/nour_essence

# Admin panel password (defaults to noureSSence2026)
ADMIN_PASSWORD=noureSSence2026
